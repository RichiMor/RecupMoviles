package com.example.recumoviles.modelo

class ModeloBuscador (resultados:ArrayList<ModeloImagen>    ) {
    var resultados:ArrayList<ModeloImagen> = resultados
        get() = field
        set(value) {
            field = value
        }
}
